[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -ra --strict-markers
markers =
    integration: needs pristine/CSR bins and published builder packs (skip if missing)
    slow: full layer stack apply (multi-second)
filterwarnings =
    ignore::DeprecationWarning
