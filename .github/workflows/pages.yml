# Deploy GitHub Pages (builder/ JSON + redirect index.html).
# The hosted builder fetches https://individualcontributor.dev/Final-Fantasy-7-Modding/builder/
name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Stage builder packs only
        run: |
          mkdir -p _site
          cp index.html .nojekyll _site/
          cp -R builder _site/builder
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - id: deployment
        uses: actions/deploy-pages@v4
