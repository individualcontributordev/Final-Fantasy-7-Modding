# 0x800E0700 is RNG table tail, not a code cave

**Date:** 2026-07-25  
**Confidence:** confirmed  
**Related:** [patch-target-field-load-reseed](2026-07-25-patch-target-field-load-reseed.md)

## Summary

`0x800E0700` falls inside **`g_field_rng_table`** (`0x800E0638`, 256 bytes → ends `0x800E0738`). Bytes pasted match table data, not free space.

## Better approach

Danger `+=` block `0x800ABB7C`–`0x800ABBD0` is **~0x54 (84) bytes**. Prefer **in-place** replace with MAX-RNG logic (nop-fill remainder) instead of a distant cave.

## Follow-ups

- [ ] Confirm exact byte length of Danger += block
- [ ] Optional: probe `0x800E0800`–`0x800E08C8` (true image end) only if stub won’t fit in-place
