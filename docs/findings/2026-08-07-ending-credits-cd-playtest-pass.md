# Ending credits CD playtest — pass

**Date:** 2026-08-07  
**Image:** `ff7_d1_playtest_ending_test` from `build_ending_credits_test_bin.py`  
**Stack:** CSR + single-disc core + manip-movies, then ending movies put back on the disc at the places the game expects; lake movie put back on its fixed disc spot; speedrun small movies restored where the long credits had covered them.  
**Fields:** left as after speedrun + single-disc (no vanilla map restore).

## Result

User playtest: **all worked fine** (lake + ending/credits path on that image).

Earlier note still applies if seen again: some rolling credit **names** can look like noise for a stretch because part of the long credits movie shares disc space with the lake movie. Scroll can still complete.

## Builder

`mods/single-disc/scripts/build_ending_credits_test_bin.py`
