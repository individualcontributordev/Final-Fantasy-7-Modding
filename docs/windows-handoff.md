# Windows handoff (agent-internal optional mirror)

**Not user-facing.** Chat has steps; `windows-last-output.txt` has COPY-PASTE values when needed.

**Current:** FORCE stub ships `thresh = g_enemy_lure / 2` (~3.13%/check at lure 16). Rebuild Encounter packs with `build_encounter_on_base.py` after stub changes.
