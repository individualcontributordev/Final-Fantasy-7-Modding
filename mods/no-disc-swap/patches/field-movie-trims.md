# FIELD movie trims (Clean no-disc-swap)

Makou script edits (not engine MOVIE stubs). Prefer trim only where D1 column is
wrong/unplayable and presentation is broken.

## ioslake3 — S0 Main — GameMoment 1398

Issue: Set next movie No57/No58 on D1 map to ONTRAIN.MOV / OPENING.BIN; real
scene is D2 LOSLAKE1.MOV + LSLMV.STR. Bugenhagen idle, no FMV.

Edit: Remove both Set next movie + both Play movie. Keep Execute scripts,
Waits, Jump to loslake1 (#637).

Status: recipe ready; apply on work bin then rebuild layer.
